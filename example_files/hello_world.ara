#!aratar 0.0.17 main

# Create an empty module
def journal
# Define an info journal.
def journal.info: Journal(SHOW, "Info").write

# Define program's entry point (set on first line of file)
def main: fn() -> Result.() {
    # Write "Hello, world!"
    journal.info["Hello, world!"]
    # Return `OK`, the program succeeded!
    OK(())
}
